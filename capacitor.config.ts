import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clevergames.app',
  appName: 'CleverGames',
  webDir: 'dist',
  android: {
    backgroundColor: '#f6f2e9',
  },
};

export default config;
